<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Register</title>
    <link
      href="https://unpkg.com/boxicons@2.1.2/css/boxicons.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/styles/global.css" />
    <link rel="stylesheet" href="/styles/forms.css" />
    <script src="/script/confirm.js"></script>
  </head>
  <body>
    <main>
      <section>
        <h1>Register</h1>
        <form action="/user/register" method="post">
          <div>
            <label for="name">Name</label>
            <div>
              <i class="bx bx-user"></i>
              <input
                type="text"
                id="name"
                name="name"
                placeholder="Enter your username"
                required
              />
            </div>
          </div>
          <div>
            <label for="email">Email</label>
            <div>
              <i class="bx bx-envelope"></i>
              <input
                type="email"
                id="email"
                name="email"
                placeholder="Enter your email, ex: example@gmail.com"
                required
              />
            </div>
            <% if(messages.emailFail) { %>
            <strong class="fail"><%= messages.emailFail %></strong>
            <% } %>
          </div>
          <div>
            <label for="password">Password</label>
            <div>
              <i class="bx bx-lock-alt"></i>
              <input
                type="password"
                id="password"
                name="password"
                placeholder="Enter your password"
                required
              />
            </div>
            <% if(messages.validation) { %>
            <strong class="fail"><%= messages.validation %> </strong>
            <% } %>
          </div>
          <div>
            <label for="confirm">Confirm password</label>
            <div>
              <i class="bx bx-lock-open-alt"></i>
              <input
                type="password"
                id="confirm"
                name="confirm"
                placeholder="Re-enter your password"
                required
              />
            </div>
            <% if(messages.confirmFailure) { %>
            <strong class="fail"><%= messages.confirmFailure %></strong>
            <% } %>
          </div>
          <% if(messages.message) { %>
          <strong class="success"><%= messages.message %></strong>
          <% } %>
          <button type="submit" class="btn">Register</button>
        </form>
        <p>You had an account ? <a href="/user/login"> Login here</a></p>
      </section>
    </main>
  </body>
</html>
